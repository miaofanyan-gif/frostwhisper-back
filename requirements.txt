# --- Web Framework & Server ---
# FastAPI 0.115+ 完美适配 Pydantic 2.9+ 和 Python 3.13
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12

# --- Async Database & ORM ---
# SQLAlchemy 2.0.35+ 修复了 3.13 下的一些细微警告
sqlalchemy>=2.0.35
# asyncmy 0.2.11 是支持 Python 3.13 源码编译的最低版本
asyncmy>=0.2.11
alembic>=1.14.0
aiomysql>=0.2.0

# --- Async Redis Cache ---
redis>=5.1.0

# --- Data Validation & Configuration ---
# 核心修复：Pydantic 2.9+ 解决了 ForwardRef 在 3.13 下的报错
pydantic>=2.9.2
pydantic[email]>=2.9.2
pydantic-settings>=2.5.0
python-dotenv>=1.0.1
argon2_cffi>=23.1.0

# --- Security & Authentication ---
# python-jose 已停止维护，但在新环境下仍可用，建议保持较新版本
python-jose[cryptography]>=3.3.0
# passlib 1.7.4 与新版 bcrypt 可能有兼容问题，保持现状或使用替代方案
passlib[bcrypt]>=1.7.4

# --- Async HTTP Client ---
httpx>=0.27.0

# --- Production Deployment ---
gunicorn>=23.0.0

# --- Dependencies ---
# 核心修复：greenlet 3.1.0+ 才是真正官方支持 Python 3.13 的版本
greenlet>=3.1.1